<Window x:Class="QAAutomationUI.PlatformSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Platform" Height="600" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F1F5F9"
        ResizeMode="NoResize">

    <Border Margin="30" Background="White" CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="20" ShadowDepth="0"/>
        </Border.Effect>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Select Testing Platform"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#1F2937"
                       Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1"
                       Text="Choose the platform you want to test on"
                       FontSize="14"
                       Foreground="#6B7280"
                       Margin="0,0,0,30"/>

            <!-- Platform Options -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <RadioButton x:Name="rbWeb"
                                 Content="🌐 Web Browser"
                                 FontSize="16"
                                 FontWeight="SemiBold"
                                 Foreground="#374151"
                                 IsChecked="True"
                                 Margin="0,0,0,10"/>
                    <TextBlock Text="Test web applications in Chrome, Firefox, or Edge"
                               FontSize="12"
                               Foreground="#9CA3AF"
                               Margin="30,0,0,20"/>

                    <RadioButton x:Name="rbDesktop"
                                 Content="🖥️ Desktop Application"
                                 FontSize="16"
                                 FontWeight="SemiBold"
                                 Foreground="#374151"
                                 Margin="0,0,0,10"/>
                    <TextBlock Text="Test Windows desktop applications"
                               FontSize="12"
                               Foreground="#9CA3AF"
                               Margin="30,0,0,20"/>

                    <RadioButton x:Name="rbMobile"
                                 Content="📱 Mobile App"
                                 FontSize="16"
                                 FontWeight="SemiBold"
                                 Foreground="#374151"
                                 Margin="0,0,0,10"/>
                    <TextBlock Text="Test iOS and Android applications"
                               FontSize="12"
                               Foreground="#9CA3AF"
                               Margin="30,0,0,20"/>

                    <RadioButton x:Name="rbCrossPlatform"
                                 Content="🔄 Cross-Platform"
                                 FontSize="16"
                                 FontWeight="SemiBold"
                                 Foreground="#374151"
                                 Margin="0,0,0,10"/>
                    <TextBlock Text="Test across multiple platforms (like Ranorex)"
                               FontSize="12"
                               Foreground="#9CA3AF"
                               Margin="30,0,0,0"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,30,0,0">
                <Button Content="Cancel"
                        Width="100"
                        Height="40"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Background="#6B7280"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Click="BtnCancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="20,10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#6B7280"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4B5563"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Continue"
                        Width="100"
                        Height="40"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnContinue_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="20,10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#2563EB"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1D4ED8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
